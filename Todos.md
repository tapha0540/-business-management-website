* Faire marcher le remove buttons commande et ajouter en haut le button statiques et supprimmer selection pour commande tabs
* ajouter un button "ajouter une produit" pour le modal update commande



* fetch imgUrl column de chaque utilisateurs et l'afficher dans la table des Utilisateurs exactement comme ce que j'ai fait avec la table Clients et Produits
* Rend le display flex et flex-direction: column pour les buttons dans la colonne Actions du table produits, du table clients et de même pour Utilisateurs
* Pour les tous tables contenant un image je vais afficher les deux premières lettres du prenom et du nom du client ou utilisateurs entourées avec un cercle avec un backgorund-color: var(--primary-color) si l'image n'est pas afficher car introuvable 
* Implementer la gestion notification qui va notifier l'utilisateur des produits qui sont en seuil critique
* mettre le header un peu en haut parce qu'il semble qu'il a trop de margin-top
